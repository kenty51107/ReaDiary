<% flash.each do |type, msg| %>
  <% if type == 'notice' %>
    <% type = 'success' %>
    <div class="alert alert-<%= type %>">
      <%= msg %>
    </div>
  <% end %>
<% end %>
<% if alert.present? %>
  <div class="alert alert-danger">
    <%= alert %>
  </div>
<% end %>